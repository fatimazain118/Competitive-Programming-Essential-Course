#include<bits/stdc++.h>
#define int long long int//in order to store more integer value then a integer can hold
#define F first
#define S second
#define pb push_back

using namespace std;

//int main(){   //here error is coming because main is defined as int and 
	          //int is defined as long long int so it's returing long long int instead of int
    
int32_t main(){

    #ifdef ONLINE_JUDGE   
    freopen("input.txt", "r", stdin); 
    freopen("output.txt", "w", stdout);
    #endif

    int n = 1e18;
    cout << n;

}
